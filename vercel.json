{
    "version": 2,
    "framework": "nextjs",
    "buildCommand": "npm install --legacy-peer-deps && npm run build",
    "outputDirectory": "out",
    "cleanUrls": true,
    "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/"
      }
    ]
}